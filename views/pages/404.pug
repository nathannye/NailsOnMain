extends ../layout.pug

block variables
  - var template = '404';
  - var metaTitle = 'Page not found';
  - var metaDescription = '';

block content
  #errorWrapper
    main
      .contentContainer
        h2= missing.data.heading
        .twemojiReplace= missing.data.emoji
          p
